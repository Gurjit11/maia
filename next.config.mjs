/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['c8.alamy.com','www.dermatrix.in','in.bmscdn.com'], // Add the domains you need here
      },
};

export default nextConfig;
